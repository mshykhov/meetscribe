[project]
name = "meetscribe"
version = "0.1.0"
description = "Automated meeting video transcription, diarization, and summarization pipeline"
requires-python = ">=3.10,<3.14"
dependencies = [
    "whisperx>=3.8,<4.0",
    "python-dotenv",
]

[project.scripts]
meetscribe = "src.process:main"
